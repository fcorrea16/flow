<!DOCTYPE html>
<html>
<head>
	<% include ../views/partials/head %>
</head>
<body>
	<h1> <% if (user.local.name != '') {%> 
					<%= user.local.name %></h1>
				<% } else { %>
					<% if (user.google.name != '') { %>
						<%= user.google.name %>
					<% } %>
			<% } %>
	</h1>
	<h4>I love flow charts because it's <%= user.local.fun_question %></h4>
	<h4> <%= user.local.location %></h4>
	<img src="<% if (user.google.name != '')%><%= user.local.picture %>">

	<% for (var i = 0; i < charts.length; i++) { %>
			<div>
				<h3><a href="/chart/<%= charts[i].id %>"><%= charts[i].chart.title %></a></h3>
			</div>
	<% } %>

</body>
</html>